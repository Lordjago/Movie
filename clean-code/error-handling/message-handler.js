// import * as toastr from 'toastr';

export function MessageHandlingStrategys(header, content, severity) {
    if(severity <= 3) {
        alert(content, header)
    }
    else {
        alert(content, header)
    }
}